module.exports = {
    extends: ['next', 'next/core-web-vitals'],
    rules: {
      'no-var': 'off',  // Disable the 'no-var' rule if it's being triggered
      // other rules can be adjusted as needed
    },
  };
  